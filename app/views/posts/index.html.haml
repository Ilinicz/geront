%h1 Listing posts
%table
  %tr
    %th Title
    %th Text
  - @posts.each do |post|
    %tr
      %td= post.title
      %td= post.content
      %td= link_to 'Destroy', post_path(post), method: :delete